# GTFS-RT Archiver Environment Configuration
# Copy to .env and customize for your deployment.

# =============================================================================
# GCS Buckets
# =============================================================================

# Required: Bucket for raw GTFS-RT protobuf snapshots
# Used by: archiver (write), dagster pipeline (read)
# For local docker-compose: use "rt-protobuf" (maps to data/rt-protobuf/)
GCS_BUCKET_RT_PROTOBUF=rt-protobuf

# Required: Bucket for compacted GTFS-RT parquet files
# Used by: dagster pipeline (write)
# For local docker-compose: use "rt-parquet" (maps to data/rt-parquet/)
GCS_BUCKET_RT_PARQUET=rt-parquet

# =============================================================================
# GCP Settings
# =============================================================================

# Required if auth used
GCP_PROJECT_ID=gtfs-archiver
GOOGLE_APPLICATION_CREDENTIALS=/path/to/account-key.json

# For local development with fake-gcs-server (docker-compose)
# Comment out for production/real GCS
STORAGE_EMULATOR_HOST=http://localhost:4443

# =============================================================================
# Archiver Service
# =============================================================================

# Logging (use "text" for local development, "json" for production)
LOG_FORMAT=text
LOG_LEVEL=INFO

# Optional: Sharding for multi-instance deployments
# SHARD_INDEX=0
# TOTAL_SHARDS=1

# =============================================================================
# Dagster Pipeline
# =============================================================================

# Required: Set to an absolute path
DAGSTER_HOME=/path/to/gtfs-realtime-archiver/.dagster_home
