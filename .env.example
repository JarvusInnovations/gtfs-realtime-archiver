# GTFS-RT Archiver Environment Configuration
# Copy to .env and customize for your deployment.

# =============================================================================
# Archiver Service
# =============================================================================

# Required: GCS bucket for raw protobuf archives
GCS_BUCKET=test-bucket

# Required if auth used
GCP_PROJECT_ID=gtfs-archiver
GOOGLE_APPLICATION_CREDENTIALS=/path/to/account-key.json

# Logging (use "text" for local development, "json" for production)
LOG_FORMAT=text
LOG_LEVEL=INFO

# Optional: Sharding for multi-instance deployments
# SHARD_INDEX=0
# TOTAL_SHARDS=1

# =============================================================================
# Dagster Pipeline
# =============================================================================

# Required: Set to an absolute path
DAGSTER_HOME=/path/to/gtfs-realtime-archiver/.dagster_home

# Required: Source bucket containing raw protobuf archives (same as GCS_BUCKET)
GCS_SOURCE_BUCKET=test-bucket

# Required: Output bucket for compacted parquet files
GCS_OUTPUT_BUCKET=test-bucket-compacted
